# A notification where the template data has been fully rendered with mustache
# Used to send notifications to the client

class: RenderedNotification
table: rendered_notification
fields:
  title: String
  body: String
  html: String?
  userNotification: UserNotification
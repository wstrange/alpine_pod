class: Section
table: sections
fields:
  id: UuidValue?, defaultPersist=random_v7
  name: String
  description: String
  location: String?
  contactInfo: String?
  updatedAt: DateTime, default=now

indexes:
  idx_sections_name:
    fields: name
    unique: true

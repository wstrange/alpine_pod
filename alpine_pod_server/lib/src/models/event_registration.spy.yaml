class: EventRegistration
table: event_registrations
fields:
  userId: int?
  eventId: int?
  registrationStatus: String # pending, confirmed, cancelled, waitlisted
  registrationDate: DateTime
  carPoolPreference: String? # drive, ride, none
  numberOfPassengers: int?
  equipmentNotes: String?
  waiverAccepted: bool
  waiverSignedDate: DateTime?
  participantNotes: String?
  # Internal management fields
  waitlistPosition: int?
  checkedIn: bool
  checkedInTime: DateTime?
  paymentStatus: String? # pending, completed, refunded
  paymentAmount: double?
  paymentDate: DateTime?
  modifiedAt: DateTime
indexes:
  user_event_idx:
    fields: userId, eventId
    unique: true
  event_status_idx:
    fields: eventId, registrationStatus
  waitlist_idx:
    fields: eventId, waitlistPosition
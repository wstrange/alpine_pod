class: EventTripLeader
table: event_trip_leaders
fields:
  eventId: int?
  user: module:serverpod_auth_core:AuthUser?, relation(onDelete=Cascade)
  assignedAt: DateTime?
indexes:
  event_leader_idx:
    fields: eventId, userId
    unique: true

class: Notification
table: notifications
fields:
  title: String
  message: String
  timestamp: DateTime
  read: bool
  memberId: int, relation(parent=members)
  attachments: String?
  eventId: int?, relation(parent=events)
indexes:
  member_idx:
    fields: memberId

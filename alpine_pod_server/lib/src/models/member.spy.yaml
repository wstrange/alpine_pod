class: Member
table: members
database: all
fields:
  # This gets set to the users id when the member is created
  id: UuidValue, defaultModel=random_v7
  firstName: String
  lastName: String
  displayName: String?
  bio: String?
  email: String
  phoneNumber: String
  membershipStatus: String, default="active"
  emergencyContactName: String
  emergencyContactPhone: String
  medicalConditions: String?
  # This comes from the User Profile - but we sync it here for efficiency
  profileImageUrl: String?
  waiverSignedDate: DateTime, default=1970-01-01T00:00:00.000Z
  # certifications and skills stored as JSON-encoded arrays in strings
  certifications: String?
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now
  registrations: List<EventRegistration>?, relation(name=member_registrations)
  managedEvents: List<EventManager>?, relation(name=member_managers)
indexes:
  member_id_idx:
    fields: id
    unique: true
  email_idx:
    fields: email
    unique: true

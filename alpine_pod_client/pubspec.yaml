name: alpine_pod_client
description: Starting point for a Serverpod client.

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  serverpod_client: 3.5.0-beta.12
  serverpod_auth_idp_client: 3.5.0-beta.12
